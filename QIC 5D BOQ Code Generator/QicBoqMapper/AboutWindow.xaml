<Window x:Class="QicBoqMapper.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QIC Tools – About" 
        Height="280" Width="360"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A"
        FontFamily="Segoe UI, Inter, Roboto"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True">

    <Window.Resources>
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#334155"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0F172A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Content -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="ABOUT QIC TOOLS" Foreground="#3B82F6" FontSize="14" FontWeight="Bold"/>
            <TextBlock Text="5D BOQ Manager Workflow Add-In" Foreground="#94A3B8" FontSize="11" Margin="0,2,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Version: 1.0.0.0" Foreground="White" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
            
            <TextBlock Text="Prepared by: Ahmed Khalaf (BIM Manager)" Foreground="White" FontSize="11" Margin="0,0,0,6"/>
            <TextBlock Text="Mobile: +966542554127" Foreground="White" FontSize="11" Margin="0,0,0,12"/>
            
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Contact Support: " Foreground="#94A3B8" FontSize="11"/>
                <TextBlock Text="engkhalaf7@gmail.com" Foreground="#3B82F6" FontSize="11" FontWeight="SemiBold" Cursor="Hand"/>
            </StackPanel>
        </StackPanel>

        <Button Grid.Row="2" Content="Close" Click="CloseButton_Click" Style="{StaticResource SecondaryButton}"/>
    </Grid>
</Window>
